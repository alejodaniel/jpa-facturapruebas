CREATE TABLE persona (IDPERSONA INTEGER NOT NULL, APELLIDO varchar(50), DIRECCION varchar(50), NOMBRE varchar(50), TELEFONO varchar(7), PRIMARY KEY (IDPERSONA))
CREATE TABLE usuario (IDUSUARIO INTEGER NOT NULL, NOMBREUSUARIO varchar(50), PASSWORD varchar(30), PERSONA_IDPERSONA INTEGER, PRIMARY KEY (IDUSUARIO))
CREATE TABLE vehiculo (IDVEHICULO INTEGER NOT NULL, AÑO varchar(30), COLOR varchar(30), MARCA varchar(30), PERSONA_IDPERSONA INTEGER, PRIMARY KEY (IDVEHICULO))
ALTER TABLE usuario ADD CONSTRAINT FK_usuario_PERSONA_IDPERSONA FOREIGN KEY (PERSONA_IDPERSONA) REFERENCES persona (IDPERSONA)
ALTER TABLE vehiculo ADD CONSTRAINT FK_vehiculo_PERSONA_IDPERSONA FOREIGN KEY (PERSONA_IDPERSONA) REFERENCES persona (IDPERSONA)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
